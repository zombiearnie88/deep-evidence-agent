# Desktop App

Desktop host for Evidence Brain.

- UI runtime: React app from `@evidence-brain/app`
- Shell: Tauri (`src-tauri/`)

## Run

- Browser-only shell: `pnpm dev:desktop`
- Native desktop shell: `pnpm dev:desktop:tauri`

# Third-Party Source Trees

This folder is reserved for source donor repositories used for reference and selective porting:

- `openkb-src/`
- `pageindex-src/`

Current local clones may still exist at repository root (`OpenKB/`, `PageIndex/`) and can be moved later.
